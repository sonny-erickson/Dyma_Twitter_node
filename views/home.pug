extends layout.pug

block content
    h1 Salut je suis le home
    div(class="flex-fill d-flex flex-column")
      if tweets
        each tweet in tweets
          div(class="card tweet-element")
            div(class="card-body")= tweet.content